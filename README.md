# 🌐 Translation App
# A simple and user-friendly Translation App that allows users to translate text between multiple languages using translation.
Prerequisites: Clear, step-by-step installation commands (pip install deep-translator).
Save the entire Python code below into a file named (translator_app.py).
How to Run: The exact command line statement used to boot up the tool (python translator_app.py).
